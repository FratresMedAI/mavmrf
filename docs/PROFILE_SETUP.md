# FratresMedAI GitHub profile checklist (manual)

GitHub’s API token for this environment cannot update profile fields (`user` scope) or pin repositories. Do these once in the browser while signed in as **FratresMedAI**:

1. Open https://github.com/FratresMedAI  
2. **Customize your pins** → pin **mavmrf** (keep up to 5 other strongest public threads)  
3. **Edit profile** → **Website** → `https://fratres-x.com` → Save  
4. Optional bio (one line): `Fratres X AI — physics-first, reviewable AI / autonomy / defensive prototypes.`

Then confirm:

- Profile website shows fratres-x.com  
- mavmrf appears in the pinned section  
- Repo homepage is already `https://fratres-x.com` (set on the mavmrf repo)
